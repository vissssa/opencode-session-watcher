# 修复 FileSink 锁增长与 HTTP 重试退避

- [x] Task 1: 修复 FileSink locks map 无界增长
    - 1.1: 将 `locks map[string]*sync.Mutex` 调整为带 `lastUsed` 元数据的 path 锁条目
    - 1.2: 在 `NewFileSink` 中初始化锁 TTL、清理间隔和时间函数
    - 1.3: 调整 `lockFor`，复用或创建 path 锁时更新最后使用时间
    - 1.4: 在写入流程完成后触发空闲锁清理
    - 1.5: 实现只删除超时且当前未被持有的 path 锁，避免影响并发写入
    - 1.6: 增加单元测试覆盖空闲锁清理与并发写入不受影响

- [x] Task 2: 修复 HTTP 重试退避时间过短
    - 2.1: 将线性退避替换为指数退避基准 `100ms, 200ms, 400ms...`
    - 2.2: 为退避时长加入随机 jitter，降低并发 worker 集中重试概率
    - 2.3: 提取退避计算函数，便于范围断言测试
    - 2.4: 保持 429、5xx、网络错误重试和普通 4xx 不重试的现有语义
    - 2.5: 增加单元测试覆盖退避范围和原有重试次数语义

- [x] Task 3: 运行验证并处理问题
    - 3.1: 运行 `go test ./...`
    - 3.2: 运行 `go test -race -timeout 30s ./...`
    - 3.3: 如测试或竞态检查失败，定位并修复本次改动导致的问题
